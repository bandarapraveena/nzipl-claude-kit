# Benchmark Mineral Intelligence drops

Hand-curated JSON from Benchmark's battery supply chain reports (subscription):

- `cell_production_YYYY.json` — actual cell production (vs capacity) by country

Row schema: see `../TEMPLATE.json`. Use `priority: 10`.
